{
  "name": "nutritrack",
  "version": "1.0.0",
  "description": "Aplicación PWA para seguimiento nutricional",
  "main": "index.js",
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "node server/server.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "start": "concurrently \"node server/server.js\" \"cd client && vite preview --port 3000 --strictPort --host 0.0.0.0\"",
    "install:all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "test": "jest"
  },
  "keywords": [
    "pwa",
    "nutrition",
    "tracking",
    "offline"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "concurrently": "^8.2.0",
    "json-server": "^0.17.3",
    "quagga": "^0.12.1",
    "vite": "^7.1.7",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node"
  }
}